.container
	.row
		.col-sm-12
			h1 Settings
	.row
		md-select.col-md-6(placeholder='Select theme', name='theme', '[ngModel]'='theme', (change)='onThemeSelect($event)')
			md-option(*ngFor='let t of themes','[value]'='t.value') {{t.label}}